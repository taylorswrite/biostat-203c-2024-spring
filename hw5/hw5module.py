def parse_actor_page(movie_df, actor_directory):
    print(f"{type(movie_df)}\n{movie_df.head(2)}\n{type(actor_directory)}:{actor_directory}")

    
    
def parse_full_credits(movie_directory):
    print(f"{type(movie_directory)}:{movie_directory}")